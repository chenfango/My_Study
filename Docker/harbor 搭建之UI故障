harbor 搭建之几大故障

1、不同的harbor之间数据同步

报错415

原因：harbor有做过升级，新版本和1.5以下的版本，接口和底层都完全不一样，会导致无法数据同步。

另外使用http 和 https 两种协议也无法同步


2、命令行docker login 成功，且log中找不到报错，但是ui 无法登陆。

原因：ui使用浏览器登陆，这时候就要看浏览器的是否有缓存，清理缓存尝试。
