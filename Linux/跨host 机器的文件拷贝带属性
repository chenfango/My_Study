当跨不同的host机器拷贝文件时候，经常会涉及文件权限的问题，加之不同host 上的都是本地用户

这时候如果使用rsync命令的话，那么权限仍会有问题，因为不同机器的本地用户的UID和GID经常会不同。

较好的解决方法是：

1、需要拷贝的文件进行打包
2、在接收的机器上创建有读写权限的目录
3、将文件拷贝到对应的目录上
4、解压缩文件，注意使用-C参数
tar zxvf   file.tar.gz  -C ./
